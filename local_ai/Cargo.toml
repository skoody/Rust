[package]
name = "local_ai"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
# Using llama-cpp-rs with hipblas feature for AMD ROCm GPU support.
# This requires the ROCm toolchain to be installed on the user's system.
llama_cpp = { version = "0.2.0", features = ["hipblas"] }
